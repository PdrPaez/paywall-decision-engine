# Entitlement model

Plans are commercial configuration; subscriptions are lifecycle projections; entitlements are internal capabilities; meters are bounded usage windows. The resolver applies trial, grace, cancellation, and expiry semantics against UTC business time. The billing provider is never queried during access decisions.

