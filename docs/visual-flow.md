# Visual flow

The XYFlow canvas is a projection of backend results: request, subject, content, subscription, entitlements, meter, policy, decision, and consumption. Node state and inspector values come from the API response, so skipped consumption and paywall branches remain inspectable without duplicating business logic in React.

