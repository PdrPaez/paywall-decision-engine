# Engineering decisions

- Internal entitlements isolate authorization from provider availability.
- Ordered Python policies keep monetization rules readable.
- Local deterministic state keeps demos offline and reproducible.
- Separate evaluate/open APIs make side effects explicit.
- HMAC mock events demonstrate verification without vendor credentials.
- XYFlow visualizes execution rather than authoring arbitrary workflows.

